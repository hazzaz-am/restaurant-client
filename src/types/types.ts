export type MenuItemType = {
	_id: string;
	name: string;
	recipe: string;
	image: string;
	category: string;
	price: number;
};

export type ItemType = {
	img: string;
	name: string;
	recipe: string;
	price: number;
};